type:behavioral

it is used to define a algorithm and allow subclasses to refined parts of the algorithm without changing the structure

concepts
    Code reuse
    common in libraries/frameworks
    works with IoC
    Algorithm emphasis
    Same algorithm
    Class based
    Compile time

Consider restrict access correctly